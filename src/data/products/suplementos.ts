import type { Product } from "@/data/types";

/**
 * Productos de la categoría Suplementos.
 * No hay productos cargados en esta categoría todavía.
 * Para añadir productos, agrégalos al array.
 */
export const suplementosProducts: Product[] = [];
